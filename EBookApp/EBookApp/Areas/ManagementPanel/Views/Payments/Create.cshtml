@model EBook.Entities.Models.Payment

@{
    ViewData["Title"] = "Create";
    Layout = "~/Areas/ManagementPanel/Views/Shared/_Layout.cshtml";
}

<h1>Ödeme Ekle</h1>

@using (Html.BeginForm("Create", "Payments", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new {@class="text-danger"})

        <label class="control-label col-md-2">Döküman</label>
        <div class="form-group">
            <div class="col-md-10">
                @Html.EditorFor(model => model.DocummentUrl, new {htmlAttibutes = new {@class = "form-control"}})
                @Html.ValidationMessageFor(model => model.DocummentUrl, "", new {@Classs = "text-danger"})
            </div>
        </div>

        <label class="control-label col-md-2">Ekleme Tarihi</label>
        <div class="form-group">
            <div class="col-md-10">
                @Html.EditorFor(model => model.CreatedDate, new {htmlAttibutes = new {@class = "form-control", @type="datetime"}})
                @Html.ValidationMessageFor(model => model.CreatedDate, "", new {@Classs = "text-danger"})
            </div>
        </div>

        <br />
        <input type="submit" value="Kaydet" class="btn btn-primary" />
        <br />
    </div>
}

